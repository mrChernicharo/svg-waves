import styled from 'styled-components';

export const Container = styled.div`
  #canvas {
    /* margin: 0 auto; */
    resize: both;
    overflow: hidden;
    min-width: 20vw;
    max-width: 100vw;
    min-height: 10vh;
    max-height: 70vh;
  }
`;
